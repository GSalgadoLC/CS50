#include <stdio.h>
#include <math.h>
#include <cs50.h>

int main(void)
{
    float amount = get_float("Dollar amount:");
    int penniers = round(amount * penniers);
    printf("Pennies %i \n, penniers");
}